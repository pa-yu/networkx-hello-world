{
  "name": "NetworkX CMU Campus Project",
  // Use the Dockerfile instead of pre-built image
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".." // Build context is project root (parent directory)
  },
  // Features to add to the dev container
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  }
}